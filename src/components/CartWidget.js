import React from 'react'
import LocalMallIcon from '@material-ui/icons/LocalMall';

export const CartWidget = () => {
    return (
        <div>
            <button>
                <LocalMallIcon 
                fontSize="small"
                color="primary"
                />

            </button>
        </div>
    )
}
